services:
  # bats_ubuntu1604:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile.ubuntu.1604.bats
  #   volumes:
  #     - ./:/usr/local/src/ez-install
  #   working_dir: /usr/local/src/ez-install
  #   command: bats --formatter tap test
  # bats_ubuntu1604_sudo:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile.ubuntu.1604.sudo.bats
  #   volumes:
  #     - ./:/usr/local/src/ez-install
  #   working_dir: /usr/local/src/ez-install
  #   command: bats --formatter tap test
  # bats_ubuntu1804:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile.ubuntu.1804.bats
  #   volumes:
  #     - ./:/usr/local/src/ez-install
  #   working_dir: /usr/local/src/ez-install
  #   command: bats --formatter tap test
  # bats_ubuntu1804_sudo:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile.ubuntu.1804.sudo.bats
  #   volumes:
  #     - ./:/usr/local/src/ez-install
  #   working_dir: /usr/local/src/ez-install
  #   command: bats --formatter tap test
  bats_ubuntu2004:
    build:
      context: ./
      dockerfile: Dockerfile.ubuntu.2004.bats
    volumes:
      - ./:/usr/local/src/ez-install
    working_dir: /usr/local/src/ez-install
    command: bats --formatter tap test
  bats_ubuntu2004_sudo:
    build:
      context: ./
      dockerfile: Dockerfile.ubuntu.2004.sudo.bats
    volumes:
      - ./:/usr/local/src/ez-install
    working_dir: /usr/local/src/ez-install
    command: bats --formatter tap test
  bats_archlinux_base-devel:
    build:
      context: ./
      dockerfile: Dockerfile.archlinux.base-devel.bats
    volumes:
      - ./:/usr/local/src/ez-install
    working_dir: /usr/local/src/ez-install
    command: bats --formatter tap test
